# Select Mobile Tyres

A Next.js website for Select Mobile Tyres, a mobile tyre fitting service.

## Getting Started

Install dependencies and run the dev server:

```bash
pnpm install
pnpm dev
```

Open [http://localhost:3000](http://localhost:3000) to view the site.

## Build

```bash
pnpm build
pnpm start
```
